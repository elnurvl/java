rootProject.name = 'java'

// Modular monolith: one deployable assembly (`app`) composed of independent modules.
// Each bounded context is its own module; `common` holds shared building blocks (e.g. Money).
include 'common'
include 'app'
